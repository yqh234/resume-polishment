interface:
  display_name: "Resume Polish Webpage"
  short_description: "Import, tailor, compare, and validate resumes."
  default_prompt: "Use $resume-polish-webpage to import my master resume, tailor it to a company and role, review each rewrite, and produce a validated resume webpage."
